name: Release
on:
  release:
    types: [released]

jobs:

  release:
    name: "Release"
    uses: skafld/github-actions/.github/workflows/release.yaml@main
    with:
      env: prod-environment
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

